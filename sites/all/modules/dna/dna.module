<?php

function dna_menu() {
    $items['dna'] = array(
        'title' => 'Helix Matcher',
        'page callback' => 'drupal_get_form',
        'page arguments' => array('dna_form'),
        'access callback' => TRUE,
        'type' => MENU_NORMAL_ITEM,
    );
}
